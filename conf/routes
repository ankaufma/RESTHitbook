# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET         /                      controllers.Application.index
POST        /felledTree            controllers.Application.addFelledTree
POST        /addFelledTrees        controllers.Application.addFelledTrees
GET         /getFelledTrees        controllers.Application.getFelledTrees
GET         /getImage              controllers.Application.getImage

# Map static resources from the /public folder to the /assets URL path
GET         /assets/*file          controllers.Assets.at(path="/public", file)
